<div class="navbar">
  <div class="logout">
  <a href="authentication/login">Logout</a>
  </div>
</div>

​<!---KPI section---><div class="helloUser"><h1>Bonjour{{{user.name}}}</h1></div>
<section id = "kpiSection">
  <div class="kpi">
        h2>Vos économies</h2>
      <h3>10 000 €</h3> <!---ce chiffre doit varier en fonction du "taxe generator"--->
    </div>
          
    <div class="kpi">
      <h2>Répas donnés</h2>
      <h3>300</h3> <!---Idem comment calculer ce chiffre--->
    </div>
          
    <div class="kpi">
      <h2>Emissions evitées</h2>
      <h3>567</h3> <!---Emissions CO2 diesel pour 1kg de déchets x n° kilos donnés--->
    </div></section>

    <!--- DONS BOARD --->
        <!---type, date de picked up, montant géneré--->
<table id="donsList">
  <caption>Dons en attente</caption>
  <thead>
  <tr>
    <th>Produit donné</th>
    <th>Date-picked up</th>
    <th>Montant</th>
  </tr>
  </thead>
  <tbody>
    <tr>
              <!---{{#each don.donStatus = picked up}}   a verifier Si le don a un status picked alors pusher dans ce tableau--->
      <td>{{don.donName}}</td>
      <td>{{don.datepickedUp}}</td>
      <td>{{Montant?}}</td> <!--- le montant a remonter par don--->
    </tr>
    <tr>
              <!---{{#each don.donStatus = picked up}}   a verifier Si le don a un status picked alors pusher dans ce tableau--->
      <td>{{don.donName}}</td>
      <td>{{don.datepickedUp}}</td>
      <td>{{Montant?}}</td> <!--- le montant a remonter par don--->
    </tr>
    <tr>
              <!---{{#each don.donStatus = picked up}}   a verifier Si le don a un status picked alors pusher dans ce tableau--->
      <td>{{don.donName}}</td>
      <td>{{don.datepickedUp}}</td>
      <td>{{Montant?}}</td> <!--- le montant a remonter par don--->
    </tr>
    <tr>
              <!---{{#each don.donStatus = picked up}}   a verifier Si le don a un status picked alors pusher dans ce tableau--->
      <td>{{don.donName}}</td>
      <td>{{don.datepickedUp}}</td>
      <td>{{Montant?}}</td> <!--- le montant a remonter par don--->
    </tr>
  </tbody>
 </table>
      <!--historic & new don--->
<div class="btns-dashboard">
      <div class="newDon">
        <h2>Vous avez quelque chose à donner? </h2>
        <div class="btn-fairenewDon">
          <a href="../dons/new.hbs">Faire un nouveau don</a><!---Idem comment calculer ce chiffre--->
        </div>
      </div>

  <div class="btn-historic">
    <a href="../dons/show.hbs">Voir l'historique total</a><!--- path menant à l'array des dons picked up--->
  </div>
</div>
<!---footer codé dans le layout-->
